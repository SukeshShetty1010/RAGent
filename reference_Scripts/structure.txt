RAG_ent/
│
├── data/                               # Source Data Wrappers & Processing
│   ├── rawg_data.py                    # Parse RAWG → structured fields
│   ├── igdb_data.py                    # Parse IGDB → structured fields
│   └── gamespot_data.py                # Parse GameSpot → structured fields
│
├── agent/                             # Vector DB + Embedding Infrastructure
│   ├── tools/
│   │   ├── data_fetcher_tool.py
│   │   ├── registry.py
│   │   └── retriever_tool.py
│   ├── base.py
│   └── core.py
│
├── ingest/                             # Ingestion + Transformation Pipeline
│   ├── loader.py                       # Load + normalize per-source canonical & docs
│   ├── merge.py                        # Merge RAWG/IGDB/GameSpot → single canonical
│   ├── chunking.py                     # Split long docs into semantic chunks
│   ├── embeddings.py                     # Split long docs into semantic chunks
│   └── upsert.py                       # insert canonical + chunks → Weaviate
│
├── vector/                             # Vector DB + Embedding Infrastructure
│   ├── schemas/
│   │   └── weaviate_gamechunk_schema.json
│   └── create_schema.py                # Create Weaviate schema
│
├── retriever/                          # Retrieval + RAG Reasoning Layer
│   └── retriever.py                    # Query Weaviate & fetch chunks
│
├── llm/                                # Remote LLM (Modal.com) Integration
│   ├── __init__.py
│   ├── modal_llm.py                    # Model deployment (Modal function)
│   └── ragent_client.py                # HTTP client for deployed LLM
│
├── docker-compose.yml                  # Local Weaviate instance + services
├── .env                                # RAWG, IGDB, GameSpot API keys
├── pyproject.toml
└── requirements.txt