# ============================================================
# RAGent — Development / Test Dependencies
# ============================================================
# Install on top of requirements.txt for local dev and CI.
# Keep these OUT of requirements.txt: Render's 512MB free-tier
# process only needs runtime deps (see CLAUDE.md).
# ============================================================

-r requirements.txt

pytest
colorama                        # used by tests/verify_engine.py
# httpx is already pinned in requirements.txt; fastapi.testclient uses it directly.

# Phase 3 — offline evaluation only (never imported on the Render request path)
ragas
datasets
langchain-groq
langchain-openai                 # Gemini RAGAS judge backend (Gemini is OpenAI-compatible)
